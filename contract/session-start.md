[aperture] ▤ Depth sorted by reader cost, not writer knowledge.

Glance (0s) = the verdict, in the reply.
Click (~2s) = the working, in a `.claude/aperture/` wiki page, linked by absolute path.
Ask (a turn) = the branch not taken, named but unexplored.

These are different content, not one answer at three lengths.
Verdict first. One idea per line. Cut throat-clearing. Shorter when in doubt.
3+ sources investigated → wiki page. Load `aperture:glance-click-ask` for the full contract.
